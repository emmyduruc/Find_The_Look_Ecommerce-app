export type ProductType = {
  _id: string
  productName: string
  price: string
  category: string[]
  productType: string
  users: string[]
  variant: string[]
  image: string
  title: string
 
}

export type Error = {
  error : string
}